pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}
rootProject.name = "2023_ScoutingApp"
include ':app', 'QRGenerator'
//project(':QrGenerator').projectDir = new File('app/libs/QRGenerator')

include ':QRGenearator'
